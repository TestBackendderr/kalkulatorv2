/**
 * Frontend-only mock API (localStorage).
 * Wszystkie dane katalogowe i kalkulacje — bez backendu.
 */
import { createMockApi } from "./kalkulatorMockApi";

const api = createMockApi();

export default api;
